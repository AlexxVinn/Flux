/** Wall thickness (px) for `collisionBounds` frame segments — keep in sync with engine framing. */
export const COLLISION_FRAME_WALL_THICKNESS = 44;

/** Default inner playable size when placing a collision frame. */
export const COLLISION_BOX_DEFAULT_SIZE = 1000;
